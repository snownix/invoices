<div class="flex flex-col w-full h-full min-h-screen">
    <!-- Bottom section -->
    <div class="min-h-0 flex-1 flex overflow-hidden">
        <!-- Narrow sidebar-->
        <.live_component 
            id="sidebar"
            module={SnownixWeb.SharedLive.SidebarComponent}
            current_user={@current_user}
        />

        <!-- Main area -->
        <%= @inner_content %>
    </div>
</div>